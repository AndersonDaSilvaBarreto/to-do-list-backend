spring:
  application:
    name: to-do-list
  datasource:
    url: ${DB_URL}
    username: ${DB_USER}
    password: ${DB_PASS}
  jpa:
    hibernate:
      ddl-auto: update
  threads:
    virtual:
      enabled: true
server:
  tomcat:
    keep-alive-timeout: 30000
    connection-timeout: 20000